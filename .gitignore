# Secrets — NEVER commit these
.env
service-account.json

# Generated data (not needed on cloud — Render uses ephemeral storage)
data/

# Dependencies
node_modules/

# Vercel (no longer using)
.vercel/

# OS files
.DS_Store
Thumbs.db